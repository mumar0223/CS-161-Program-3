# 🍔 Simple Restaurant Menu

This project is a **basic restaurant menu webpage** built using only **HTML** and **inline CSS** (no separate CSS file).  
It’s a simple layout that lists food categories like *Starters*, *Main Course*, and *Desserts*.

---

## 🎨 Features

- Clean and centered menu design  
- Warm background color for a cozy restaurant feel  
- Easy-to-read food categories and prices  
- Uses only inline styles for simplicity  

---

## 🧩 Code Explanation

- The `<body>` uses `background-color: #fff8e1;` to create a soft yellow background.  
- Each section (Starters, Main Course, Desserts) is created using an `<h2>` tag with an emoji.  
- Menu items are simple `<p>` tags showing the dish name and price.  
- The content is wrapped inside a `<div>` with `width: 60%; margin: auto;` to center it on the page.  
- The main title (`<h1>`) is colored orange using `color: #d35400;`.

---

## 📸 Example Layout (Text View)

